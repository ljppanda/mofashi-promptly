# 模法师 Promptly · Design Context

> Persisted per Impeccable skill workflow. Used to keep the redesign coherent across sessions.

## Product
- **模法师 Promptly** — AI 提示词模板架构师（F1 生成模板骨架 → F2 实例化为成品提示词）。
- Vite + TS 零运行时框架前端，hash 路由；Node22 TS 后端。本仓库仅含前端视觉层。

## Audience
- 中文创作者 / 职场人 / 开发者 / 小团队：想要"可复用、生产级"提示词，但不想从零写。
- 隐私敏感：API Key 仅存浏览器本地，强调"数据不出域"。

## Brand personality
- 专业、克制、可信，带一点东方文人气质（不是科技蓝、不是 AI 紫）。
- 编辑感：像一本排版讲究的刊物，而非 SaaS 仪表盘。

## Design direction (agreed 2026-08-12)
- **风格**：东方墨韵 · 编辑感（Eastern ink-rhythm, editorial）。
- **主题**：浅色优先。
- **范围**：设计系统 + 首页 / 社区 / 导航先行；其余页面通过共享类继承换肤。

## Tokens (东方墨韵调色板 — 定义在 site/src/styles.css :root，保留原变量名仅重定向)
- 宣纸底 `--bg:#f5f0e3`；卡片 `--surface:#fffdf6`；墨字 `--ink:#1d1812`；次级 `--slate:#6b6253`；弱化 `--muted:#9c9080`；描边 `--line:#e9ddc8`。
- 主强调（朱砂红）`--brand:#b5362a` / `-600:#9c2c21` / `-700:#7f2318` / `-50:#f7e8e2` / `-100:#efd5cc`。
- 次强调（描金）`--amber:#b8893f` / `-600:#9c7232`（复用 `--violet` 旧名承载）。
- 渐变单色相：按钮/步骤 `--grad-brand: linear-gradient(135deg,#b5362a,#8f261c)`；Hero 暖宣纸径向 `--grad-hero`。
- 标题衬线：`--font-display: "Songti SC","STSong","SimSun","Noto Serif SC",Georgia,serif`（系统衬线栈，无外部字体依赖，满足离线/隐私约束）。
- 阴影暖调化（去蓝），圆角收紧（14/10px）。

## 反模式（明确规避）
- 不用 AI Slop：紫蓝渐变、Inter 默认、泛冷灰、过度阴影。
- 不引入外部字体 CDN（隐私/离线优先）。
- 行业封面占位图改用低饱和大地色系（赭/竹青/酱褐/青碧/墨灰），不再用霓虹粉蓝。

## 已落地改动（R2 设计系统重做）
- styles.css 全量重写：令牌 + 组件类 + 衬线标题。
- 清理硬编码紫/冷灰 hex：home / community / detail / models / guide / auth / ui / settings / traces。
- 社区封面 INDUSTRY_PH 改为墨韵大地色梯度。
- 标签 chips（text-slate-500 bg-slate-100）→ 统一 `.tag` 暖陶。
