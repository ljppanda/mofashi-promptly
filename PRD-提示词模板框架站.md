# 提示词模板框架站 · 产品需求文档（PRD / v1）

> 版本：v1.8 ｜ 状态：待评审 ｜ 日期：2026-07-24
> 一句话定位：**一个跨行业、AI 原生的提示词"框架模板"站——程序员（及后续普通用户）浏览或一句话生成带占位的可填模板，填完一键拿到成品提示词；支持模板预览。**
> v1.3 修订：评分/打分与评分最高榜单**移回 DEFERRED**（非主要功能，后续实现）；v1 聚焦基础可用闭环。当前先交付可运行原型供试用。
> v1.4 修订：① island 选型**已决**——v1 维持 vanilla 零构建，仅 v2 出现重交互时升级 Astro + Svelte island（非 React）；② 设置页新增「测试连接」按钮（填 Key 后一键验证可用性 + 回显实际调用模型），模型清单刷新为 2026-07 各厂商在役最新版。
> v1.5 修订：**自定义生成的模板可由用户自行决定所属行业分类**——详情页新增可编辑「所属分类」下拉（8 主力行业 + “其他”），默认取生成时选定的行业，用户可自由改到任意分类，收藏时一并写入个人库；详情页对模板做深拷贝，避免改动污染全局。
> v1.6 修订：**① F1 一句话生成改为流式输出**——首页生成框下方实时显示模型逐字输出（含完成提示），用户可直观看到模型在运行；**② 代理开关（USE_PROXY）落地**——设置页新增「通过代理服务器调用」开关 + 代理地址；开启后所有 LLM 请求（含流式）改走 `site/server.js` 的 `/relay` 端点，规避浏览器跨域(CORS)、Key 由服务端转发。本地统一用 `node server.js` 托管静态 + 代理（替代 `python -m http.server`）。
> v1.7 修订：**详情页新增「保存到本地」**——把填好的成品提示词导出为文件，模型可直接读懂。格式三选一：**JSON**（OpenAI chat completions 请求体 `{model, messages:[{role:"user",content}]}`，可直接 POST 调 API）、**Markdown**、**纯文本**；文件名取模板 slug。与「复制」（剪贴板）并列，满足离线存档 / API 直调场景。
> v1.8 修订（用户追加，5 项）：① **导入闭环**——顶部导航与「我的模板」页均提供「📥 导入」入口，读取本地 JSON（支持“模板定义 JSON”含 prompt / “成品 OpenAI JSON”含 messages 两种格式），载入详情页可继续编辑 / 收藏；② **示例模板文件**——`site/samples/` 下生成 4 个行业示例 JSON（法律顾问/代码审查/家庭医生/商品文案），首页提供下载入口；③ **「我的模板」按分类分组**展示（8 主力行业 + 其他，沿用 ALL_INDUSTRIES 顺序）；④ **流式「停止生成」**——生成中出现「停止生成」按钮，点按以 AbortController 中断；⑤ **生成后展示 token 消耗**——首页 live 框与详情页均显示「输入/输出/缓存命中/缓存写入/合计/耗时」，由各厂商流式 usage 归一化（OpenAI 走 stream_options.include_usage；Claude 取 message_start/message_delta；Gemini 取 usageMetadata）。下载新增「模板 JSON」格式（含 variables/prompt，可再导入）。

---

## 1. 背景与市场

### 1.1 痛点
- 市面缺少"通用模板提示词"，多数开发者/普通用户每次都要从零想提示词，成本高、质量不稳定。
- 现有提示词库（DevTk、AiShort、PromptDen、DeepSeek 提示词平台等）多为**"展示文本 + 手动替换 `{{变量}}` + 复制"**，交互原始、无预览、无评分。
- 用户表达能力参差，纯"一句话直接生成最终提示词"的工具产出方差大、不可复用。

### 1.2 竞品格局（已核实，2026-07）
| 产品 | 形态 | 弱点 |
|---|---|---|
| DevTk / aipromptlibrary | 静态模板库，`{{变量}}` 手动替换 | 无交互填表、无 AI、无评分 |
| AiShort / DeepSeek 提示词平台 | 20+ 场景中文库，一键复制 | 复制文本为主，无智能填充/评分 |
| FlowGPT / PromptBase | 社区/市场 | 质量参差、偏交易 |
| PromptPerfect | 提示词优化器 | 非模板框架、不解决"发现" |

**结论**：赛道拥挤，但"交互式填表 + AI 一句话生成/智能补全 + 预览/评分"几乎无人做透。这是我们的差异化主轴。

### 1.3 差异化主轴
- **D1 交互式填表**：选模板 → 填变量（下拉/多选/文本）→ 实时拼装 → 一键复制。
- **D5 AI 原生**：一句话生成"可填模板草稿" + 补全按钮给出候选。
- **D6 预览 + 评分**：卡片预览提示词骨架、用户打分、首页展示评分最高，形成质量飞轮。
- 价值场景：**程序员做跨行业 AI 产品时，来这找/生成对应行业的提示词框架**（如法律顾问、家庭医生），不用从零想；自己的开发工作另有"编程开发"分类兜底。

---

## 2. 目标用户（两条独立轴，勿混淆）

### 轴 1 · 谁来用这个软件（工具可用性）
- **v1：程序员**（技术用户，能自托管、配 LLM Key）。
- **后续（"全民通用"）**：让**不懂技术的普通人也能轻松上手**——UI/UX 简化：隐藏 `{{变量}}` 术语、字段用大白话、后续可加"免 Key 托管模式"。关乎**工具好不好用**，与模板内容无关。

### 轴 2 · 模板讲哪个行业（内容领域）
- 模板内容**横跨所有行业**，因为程序员会在各行各业做产品，需要对应行业的提示词框架。
- **通用行业模板（主体 / 核心）**：法律 / 医疗健康 / 职场办公 / 教育培训 / 电商运营 / 金融 / 写作创作。例如"法律顾问""家庭医生""合同审查"。
- **编程自身模板（兜底 / 测试用，次要）**：代码审查 / 调试 / 重构 / 单测 / 文档生成。属"编程开发"行业分类下的子项，程序员给自己开发工作用，**不是通用行业模板的主体**。

> 关键纠正：法律/医疗模板**不是给律师/医生用的**，而是给**身处这些行业的程序员**用的。

---

## 3. 核心功能清单（v1 · IN）

| # | 功能 | 说明 |
|---|---|---|
| F0 | 行业×任务浏览 | 一级行业（8 个）× 二级任务导航 |
| F0 | 全文搜索 + 标签过滤 | 语言/框架/模型/场景等多选标签 + 关键词搜索 |
| F0 | 模板预览 | 卡片显示提示词骨架截断预览（{{变量}} 以可读 label 呈现） |
| F1 | 一句话生成模板 | 用户**指定行业** + 一句话 → AI 返回结构化模板草稿 → 落入填表流程 |
| F2 | 智能补全 | 填表时点"补全"，AI 对每个未填占位返回 2–3 候选，点选填充 |
| F3 | 交互式填表 | 4 种变量输入（文本域/单行/下拉/多选）→ 实时预览拼装 |
| F4 | 一键复制 | 复制成品提示词到剪贴板 |
| F5 | 我的模板 | localStorage 持久化（AI 生成的 + 收藏的） |
| F6 | 设置 | 配置自有 LLM Key + 模型选择 |
| F7 | 贡献入口 | GitHub PR 提交新模板，公共库人工精选 |
| F8 | 评分 / 打分 | 用户对模板打 1–5 星（匿名，无需账号），聚合存储于 serverless KV |

**DEFERRED（v2+）**：**评分/打分 与 评分最高榜单**（非主要功能，后续实现）、账号/云端同步、平台托管 Key（免 Key 模式）、社区审核流 / 版本、跨工具嵌入（浏览器插件浮窗 / Coze，即"claw"）、**工具对非程序员的易用性优化（全民通用 UX）**、实时补全、拖拽组装、模板效果自动评测。

---

## 4. 信息架构 / 页面

```
/                 首页：搜索框 + 一句话生成入口 + 行业导航 + 评分最高榜单 + 热门预览
/industry/[行业]   行业页：该行业任务列表 + 标签过滤 + 本行业评分最高
/t/[模板slug]      模板详情：提示词骨架预览 + 变量表单 + 实时预览 + 复制 / 补全 / 存我的模板 / 打分
/my               我的模板（localStorage）
/settings         设置：LLM Key + 模型
/contribute       贡献说明（PR 模板 + 规范）
```

---

## 5. 模板数据格式规范

每个模板 = 一个 Markdown 文件，YAML frontmatter 声明元数据与变量，正文为提示词骨架（用 `{{变量名}}` 占位）。新增可选字段 `rating`（初始种子分，由维护者填；用户评分写入 KV 聚合）。

### 5.1 通用行业模板示例（主体）
```markdown
---
title: 法律顾问
industry: 法律
task: 法律顾问
summary: 让 AI 扮演某法域的法律顾问，就用户问题给出结构化法律分析与建议
tags: [合同, 合规, 咨询]
model: general
author: your-name
variables:
  - name: jurisdiction
    label: 适用法域
    type: select
    options: [中国大陆, 中国香港, 美国, 欧盟]
    required: true
  - name: role
    label: 顾问角色
    type: text
    required: true
    placeholder: 如：初创公司外部法务
  - name: question
    label: 用户问题
    type: textarea
    required: true
    placeholder: 描述你遇到的法律情境或问题
  - name: scope
    label: 输出范围
    type: multiselect
    options: [风险点, 合规建议, 条款草稿, 下一步行动]
    required: false
---
你是一名持有 {{jurisdiction}} 执业资格的资深法律顾问，当前角色是 {{role}}。
请就以下问题给出结构化分析，覆盖 {{scope}}：

问题：
{{question}}

要求：区分"法律建议"与"实操建议"，标注不确定性，必要时提示咨询持证律师。
```

### 5.2 编程自身模板示例（兜底/测试用，属"编程开发"行业）
```markdown
---
title: 代码审查
industry: 编程开发
task: 代码审查
summary: 让 AI 扮演资深工程师，按指定关注点审查代码
tags: [python, go, typescript, security, performance]
variables:
  - name: language
    label: 编程语言
    type: select
    options: [Python, Go, TypeScript, Java, Rust]
    required: true
  - name: focus_areas
    label: 关注重点
    type: multiselect
    options: [性能, 安全, 可读性, 边界情况, 测试覆盖]
    required: false
  - name: code
    label: 待审查代码
    type: textarea
    required: true
    placeholder: 在此粘贴代码
---
你是一名资深 {{language}} 工程师。请审查下面这段代码，重点关注 {{focus_areas}}。
代码：
{{code}}
```

### 5.3 变量类型（4 种）
| type | 渲染 | 适用 |
|---|---|---|
| `textarea` | 多行文本框 | 代码、长文本、用户原文 |
| `text` | 单行输入 | 短值（角色名、主题） |
| `select` | 下拉单选 | 枚举值（法域、语言、输出格式） |
| `multiselect` | 多选标签 | 可叠加的关注点 |

### 5.4 生成索引
构建时扫描 `src/content/templates/*.md`，生成 `templates.json`（含 frontmatter + 正文 + 聚合评分），供前端无后端读取（评分在客户端拼合 KV 数据）。

---

## 6. AI 调用协议

### 6.1 Key 与模型（采纳方案 C）
- **v1 用户自带 Key**：存浏览器 `localStorage`。
- 支持 18 家服务商、可选/自定义具体模型（见 §6.1.1）。
- **前端直连 LLM**（v1，零后端）为默认；设置页提供「通过代理服务器调用」开关（`useProxy`）+ 代理地址（`proxyBase`）。开启后所有请求（含流式）改走代理的 `/relay` 端点，规避浏览器跨域(CORS)、Key 由服务端转发，不暴露给前端直连。配套代理即本仓库 `site/server.js`（`node server.js` 同时托管静态与 `/relay`，零依赖）。
- **F1 流式输出**：一句话生成时首页实时显示模型逐字返回，done 后自动跳转填表；非流式 `callChat` 仍用于「测试连接」与 F2 补全。

### 6.2 F1 一句话生成模板（行业由用户指定）
- **输入**：用户先**从下拉选择行业**（8 个行业 + "其他"），再输入一句话（如"法律顾问，帮我生成合同审查的提示词框架"）。
- **系统提示词**：要求模型以**严格 JSON** 返回模板草稿，且 `industry` 必须等于用户所选：
  ```json
  {
    "title": "...", "industry": "法律", "task": "...", "summary": "...",
    "variables": [
      {"name":"...","label":"...","type":"select|multiselect|textarea|text",
       "options":[...], "required": true/false}
    ],
    "prompt": "你是一名...{{var1}}..."
  }
  ```
- **落点**：渲染成填表界面，用户改/填后生成成品。**不自动进公共库**，可"存我的模板"。生成时选定的行业为默认分类，用户可在详情页「所属分类」下拉中自由改到任意分类（含"其他"），收藏时一并写入。
- **设计意图**：生成"可填框架"而非"最终答案"——抹平表达能力方差、产物可复用、与 A 流程统一。

### 6.3 F2 智能补全
- **触发**：用户在模板详情页点"补全"按钮（非实时，省请求）。
- **输入**：当前已填变量 + 模板骨架。
- **输出**：对每个**未填**变量返回 2–3 个候选（select/multiselect/text 给值；textarea 给提示词片段）。
- **UI**：以可点选 chip 呈现，用户点选填入或自行编辑。

---

## 7. 分类体系（已锁定）

- **一级：行业（industry）**——8 个，已锁定：
  `法律` / `医疗健康` / `职场办公` / `教育培训` / `电商运营` / `金融` / `写作创作` / `编程开发`
- **二级：任务（task）**——每行业 **3 个** v1 种子任务（共 24 个模板）。示例：
  - 法律：法律顾问 / 合同审查 / 条款解释
  - 医疗健康：家庭医生 / 症状初判 / 健康科普
  - 职场办公：周报生成 / 邮件起草 / 会议纪要
  - 教育培训：课程大纲 / 习题生成 / 批改反馈
  - 电商运营：商品文案 / 客服话术 / 活动方案
  - 金融：理财科普 / 风险评估 / 研报摘要
  - 写作创作：爆款标题 / 脚本大纲 / 风格改写
  - 编程开发（兜底）：代码审查 / 调试修复 / 重构优化
- **标签（facet，可多选过滤）**：语言 / 框架 / 目标模型 / 适用场景
- **搜索**：标题/摘要/正文全文

---

## 8. 技术架构

- **v1（当前原型）：零构建 vanilla 静态站**
  - 纯 HTML + 原生 JS（Tailwind 经 CDN），无打包、无依赖、无后端。双击 `index.html` 即可运行（仅 Tailwind CDN 需联网）。
  - **本地开发/代理服务器**：`site/server.js`（Node 内置模块，零依赖）——`node server.js` 同时托管静态文件与 `/relay` LLM 中继代理，默认端口 8000；开启代理开关后前端经它转发 LLM 请求。替代 `python -m http.server`（后者无 /relay）。
  - 模块：`src/templates.js`（种子模板）、`src/store.js`（localStorage 封装）、`src/llm.js`（多服务商 LLM 直连 + 流式 + 代理路由 + 测试连接）、`src/app.js`（哈希路由 + 各页渲染 + 流式生成 + 设置页测试连接/代理开关）。
  - **选型理由**：v1 交互轻（浏览 / 搜索 / 填表 / 实时预览 / 复制 / 补全 / 流式生成），原生 JS 已足够，且部署摩擦最小、可直接交付试用。
- **v2（仅在出现重交互时升级）：Astro + Svelte island**
  - **升级触发条件**：出现明显复杂共享状态 / serverless KV 评分 / 拖拽组装模板 / 实时协作编辑器等重交互痛点时，再引入框架，避免为用框架而用框架。
  - **island 框架选型（已定，2026-07-24）：推荐 Svelte，而非 React。**
    - 理由：本站立交互动轻、无复杂共享状态，Svelte 编译期优化、无虚拟 DOM、包体更小、hydrate 更快；填表/双向绑定（本核功能）样板显著少于 React 的 `useState`/`useEffect`。React 仅在「团队栈锁死 React」或「确认重度交互且全 React 人才」时更优，但性价比不及 Svelte。两者 Astro 集成均为一流，平手。
    - 模板仍按 Markdown 内容集合管理，交互部分（填表 / AI / 评分）写成 Svelte island（`client:*` 才会 hydrate），保留 Astro 纯静态 / SEO / 零 JS 默认优势。
- **状态/存储**：
  - "我的模板"与 LLM Key：浏览器 `localStorage`（无后端）。
  - **v1 全站纯静态、零后端**；评分（v2）届时引入轻量 serverless KV，不影响 v1 架构。
- **部署**：纯静态站，可部署 GitHub Pages / Vercel / 任意静态托管 / 自托管（v1 零后端；v2 评分可选加 serverless 函数）。
- **协议**：MIT 开源。
- **目录建议（v2 升 Astro 时参考）**：
  ```
  src/content/templates/*.md      # 模板库（按行业分子目录）
  src/components/FillForm.svelte   # 交互式填表 + 预览（Svelte island）
  src/components/AIGenerate.svelte # F1 一句话生成（含行业选择）
  src/components/Rating.svelte     # 打分 + 榜单
  src/lib/llm.ts                   # LLM 直连 + 代理开关
  src/lib/store.ts                 # localStorage 封装
  src/lib/ratings.ts               # KV 读写封装
  api/rate.ts                      # serverless 评分端点
  public/templates.json            # 构建生成的索引
  ```

---

## 9. 内容治理与贡献

- 公共库：**人工精选维护**，质量优先。
- 贡献方式：**GitHub PR** 提交新 `.md` 模板（仓库提供 PR 模板与格式校验）。
- AI 生成内容：**仅入个人库**，不自动进公共库（防污染）。
- 评分用于排序展示，不参与入库门槛（DEFERRED 后续可加社区审核/版本）。

---

## 10. v1 任务拆解（里程碑）

| 里程碑 | 内容 | 交付 |
|---|---|---|
| M1 | 脚手架 + 内容集合 + 构建索引 | Astro 工程跑通，含 24 个跨行业示例模板 |
| M2 | 首页 / 行业页 / 搜索 / 标签过滤 | 浏览发现可用 |
| M3 | 模板详情：骨架预览 + 填表 + 实时预览 + 复制 | F0/F3/F4 可用 |
| M4 | 设置页（Key）+ F1 一句话生成（用户指定行业）+ F2 补全 | AI 层可用 |
| M5 | 我的模板（localStorage） | F5 可用 |
| M6 | 贡献说明 + README + 部署配置 | 可开源发布 |

---

## 11. 风险与开放问题

1. **AI 生成模板质量**：落个人库不污染公共，前端做 JSON 校验与兜底。
2. **前端直连 LLM 的 CORS / 密钥安全**：v1 用户自有 Key 可接受；代理开关作为 B 方案预留。
3. **评分后端让步**：引入 serverless KV（唯一后端），需部署者配置环境变量；若坚持零后端，则评分仅个人可见、无全局榜单（与需求冲突，故采用 KV）。
4. **行业模板深度**：v1 每行业 3 任务，需保证通用行业（法律/医疗/职场…）质量，避免"只有代码审查"观感。

---

## 12. 评审待确认项（剩余）

- [x] React island 还是 Svelte island？**【已决 2026-07-24】推荐 Svelte（非 React）**；v1 维持 vanilla 零构建原型，仅在 v2 出现重交互（评分 KV / 拖拽 / 协作）时升级 Astro + Svelte island。
- [x] "其他"行业（用户自定义）生成的模板，落个人库时行业标签如何处理？**【已决 2026-07-24】** 详情页提供可编辑「所属分类」下拉（含 `其他`），用户自定义生成的模板可自行归类到任意分类（含 `其他`），收藏时写入个人库；AI 生成内容仅入个人库、不进公共库（见 §9）。
