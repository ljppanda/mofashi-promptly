{
  "name": "mofashi-promptly-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "模法师-Promptly 前端（Vite + TypeScript，零运行时框架，原生 ES 模块）。",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "start": "npm --prefix server run start",
    "serve": "npm --prefix server run serve"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
